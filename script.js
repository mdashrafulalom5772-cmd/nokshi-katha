const phone="8801864259146", products=[1,2,3,4,5].map((n,i)=>({name:`নকশী কাঁথা — ডিজাইন ${n}`,price:"৳৮৯০",cat:i<3?"kantha":"home",img:`images/product-${n}.jpg`}));let filter="all";const grid=document.querySelector('#grid'),search=document.querySelector('#search');function render(){let q=search.value.toLowerCase();grid.innerHTML=products.filter(p=>(filter==='all'||p.cat===filter)&&p.name.toLowerCase().includes(q)).map(p=>{let msg=encodeURIComponent(`আসসালামু আলাইকুম, আমি ${p.name} অর্ডার করতে চাই। দাম: ${p.price}`);return `<article class="card"><img src="${p.img}" alt="${p.name}"><div class="card-body"><h3>${p.name}</h3><div class="price">${p.price}</div><a class="order" target="_blank" href="https://wa.me/${phone}?text=${msg}">WhatsApp-এ অর্ডার</a></div></article>`}).join('')}document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;render()});search.oninput=render;render();const slides=[...document.querySelectorAll('.slide')],dots=document.querySelector('#dots');let current=0;slides.forEach((_,i)=>{let d=document.createElement('span');d.className='dot'+(i?'':' active');d.onclick=()=>show(i);dots.appendChild(d)});function show(i){current=(i+slides.length)%slides.length;slides.forEach((s,j)=>s.classList.toggle('active',j===current));document.querySelectorAll('.dot').forEach((d,j)=>d.classList.toggle('active',j===current))}document.querySelector('#prev').onclick=()=>show(current-1);document.querySelector('#next').onclick=()=>show(current+1);setInterval(()=>show(current+1),4500);
